import { PlayerClass } from "../../../../types/object.types";

export interface ClassAvatarProps
{
    style?: React.CSSProperties;
    playerClass: PlayerClass;
    selected?: boolean;
}

export interface ClassAvatarState
{
}
